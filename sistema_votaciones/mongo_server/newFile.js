const { app, port } = require('./server');

Uri.parse('http://mongodb+srv://usley:140922USLEY@cluster0.domvtuq.mongodb.net/mydatabase?retryWrites=true&w=majority', 3000 / login, ")",
  Uri.parse('http://<DIRECCION_IP_DEL_SERVIDOR>:3000/register'),
  m,
  app.listen(port, () => {
    console.log(`Servidor escuchando en http://localhost:${port}`);
  }));
